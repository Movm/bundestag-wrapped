import type { SlideConfig, CardConfig } from './types';

export const toneAnalysisConfig: SlideConfig = {
  emoji: '🎭',
  title: 'Fraktions-Persönlichkeiten',
  subtitle: 'Die Kommunikationsstile der Parteien',
  filename: 'bundestag-wrapped-tone',
};

export const toneBubbleConfig: CardConfig = {
  width: 180,
  height: 200,
  gap: 16,
  radius: 20,
};
